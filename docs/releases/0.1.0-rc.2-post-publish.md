# 0.1.0-rc.2 Post-publish Verification

## Result

NoHardText `0.1.0-rc.2` was published and verified successfully.

## Package

```txt
@nohardcoding/nohardtext@0.1.0-rc.2
```

## Install

```bash
npm install -D @nohardcoding/nohardtext@rc
```

## Binary

```bash
npx nohardtext --version
```

Expected:

```txt
NoHardText 0.1.0-rc.2
```

## Dirty Test

Input:

```tsx
export default function App() {
  return <button>Save</button>;
}
```

Command:

```bash
npx nohardtext scan src
```

Verified output included:

- `Findings: 1`
- `Can I ship? No`
- `NHT1001 - JSX Text`
- `Hardcoded JSX text found: "Save"`

## JSON Test

Command:

```bash
npx nohardtext scan src --json --output nohardtext-report.json
```

Verified:

- command completed
- JSON file was created

## Clean Test

Input:

```tsx
function t(key: string): string {
  return key;
}

export default function App() {
  return <button>{t("actions.save")}</button>;
}
```

Command:

```bash
npx nohardtext scan src
```

Verified output included:

- `Findings: 0`
- `Can I ship? Yes`
- `Localization grade: AAA`
- `Localization score: 100 / 100`

## Notes

The package works correctly when installed from npm in a clean project.

The first public RC is ready for external testing.
